name: Setup Token Mappings

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (preview only)'
        required: true
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
      batch_size:
        description: 'Mappings per transaction'
        required: false
        default: '100'
      max_gas_gwei:
        description: 'Max gas price (gwei)'
        required: false
        default: '0.05'
      resume_from:
        description: 'Resume from batch number'
        required: false
        default: '0'

permissions:
  contents: write

jobs:
  setup-mappings:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Setup token mappings
        env:
          TURNKEY_API_PUBLIC_KEY: ${{ secrets.TURNKEY_API_PUBLIC_KEY }}
          TURNKEY_API_PRIVATE_KEY: ${{ secrets.TURNKEY_API_PRIVATE_KEY }}
          TURNKEY_ORGANIZATION_ID: ${{ secrets.TURNKEY_ORGANIZATION_ID }}
          TURNKEY_SIGN_WITH: ${{ secrets.TURNKEY_SIGN_WITH }}
          RPC_URL: ${{ secrets.RPC_URL }}
          DRY_RUN: ${{ inputs.dry_run }}
          BATCH_SIZE: ${{ inputs.batch_size }}
          MAX_GAS_PRICE_GWEI: ${{ inputs.max_gas_gwei }}
          RESUME_FROM: ${{ inputs.resume_from }}
        run: npx tsx scripts/setup-token-mappings.ts

      - name: Regenerate manifest
        if: inputs.dry_run == 'false'
        run: npx tsx scripts/generate-manifest.ts

      - name: Commit updated manifest
        if: inputs.dry_run == 'false'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add docs/manifest.json docs/manifest-index.json
          git diff --staged --quiet || git commit -m "chore: update manifest after token mappings [skip ci]"
          git push || true
